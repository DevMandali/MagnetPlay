<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <property name="LOG_DIR"     value="logs"/>
  <property name="LOG_FILE"    value="spring"/>
  <property name="MAX_SIZE"    value="20MB"/>
  <property name="MAX_HISTORY" value="14"/>

  <!-- Console -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) %cyan(%logger{36}) : %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Rolling file -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/${LOG_FILE}.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>${MAX_SIZE}</maxFileSize>
      <maxHistory>${MAX_HISTORY}</maxHistory>
      <totalSizeCap>200MB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} : %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Suppress noisy frameworks -->
  <logger name="io.grpc"                          level="WARN"/>
  <logger name="reactor.netty"                    level="WARN"/>
  <logger name="org.springframework"              level="WARN"/>
  <logger name="io.netty"                         level="WARN"/>

  <!-- App code at INFO (override with LOG_LEVEL env var) -->
  <logger name="org.devMandali.magnetPlay" level="${LOG_LEVEL:-INFO}"/>

  <root level="WARN">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </root>

</configuration>
